{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "module": "preserve",
    // Vite compiles JSX, so tsc only needs to type-check it.
    "jsx": "preserve",
    "incremental": true,
    "types": ["node"]
  },
  "include": ["src/**/*", "env.ts"]
}
